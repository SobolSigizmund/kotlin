import java.io.*;

File file = new File(basedir, "target/test-classpath-" + PROJECT_VERSION + ".jar");
if (!file.exists() || !file.isFile()) {
    throw new FileNotFoundException("Could not find generated JAR: " + file);
}
